# 2026-05-19 19:43 PT — Ponder backend outage

## Intended unit of work
`GET /agents/binding/7593` — ERC-8004 binding lookup; sibling of /agents/info covered 2026-05-18 0843.

## Observed
All Ponder-backed endpoints return HTTP 502 with body:

```json
{"error":"Ponder API request failed: The operation was aborted due to timeout"}
```

Probed (in order):
- `/holders/0x523Eff3d…dde5` → 502 (wallet refresh)
- `/holders/…` retry after 3s → 502
- `/agents/binding/7593` → 502
- `/normie/7593/traits` (sanity, previously cached + 200) → 502

Static asset still serves fine:
- `/llms.txt` → 200, normal body

## Response headers (representative — /agents/binding)
```
HTTP/2 502
server: Vercel
x-vercel-cache: MISS
x-vercel-id: sfo1::iad1::lz4lg-1779245102185-91d0c22f13b4
x-ratelimit-limit: 60
x-ratelimit-remaining: 59
content-type: application/json
```

Rate limit untouched (remaining=59), so failures are not throttling — pure upstream Ponder.

## Interpretation
Vercel edge is up; the Ponder indexer that backs the dynamic routes is unreachable from the API. Nothing on our side. Wallet snapshot stays at 2026-05-18 19:45 PT (still `["7593"]`).

## Next fire
- If endpoints recover: pick up `/agents/binding/7593` as planned.
- If still down: try `/agents/list` (might use a different backend path), else park survey for one cycle and start on the persona-aware reply skill build using the cached `data/agents-info-7593.json`.

## Action this fire
None beyond the note + journal line. No Telegram (upstream outage, not Melted-blocker).
