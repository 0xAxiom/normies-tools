# /agents/list — survey close

- Time: 2026-05-20 19:43 PT
- Endpoint: `GET https://api.normies.art/agents/list`
- Status: 200, 577ms, 5609 bytes

## Shape

```json
{
  "items": [
    {
      "agentId": "33168",
      "tokenId": "9076",
      "name": "Pavel",
      "type": "Human",
      "registeredBy": "0x6b5bf95008840faf2d6c8051b5f0188475d59917",
      "registeredAt": "1779321299",
      "txHash": "0x1c1b38f7…6638ee"
    },
    …
  ],
  "hasMore": true
}
```

- Page size: 24 items
- Order: descending `agentId` (33168 → 33144 on this page)
- Pagination: `hasMore: bool` only — no cursor field surfaced; next-page param unknown. Candidates to probe later (one per fire): `?cursor=`, `?before=`, `?offset=`, `?page=`. Don't guess; check `/llms.txt` or repo first.
- Field set per item: `agentId` (string), `tokenId` (string, points back at the Normie), `name`, `type` (Human/Alien/…), `registeredBy` (lowercase 0x addr), `registeredAt` (unix seconds), `txHash`.

## Cross-reference with our Normie

- Our Normie #7593 / agentId 32811 (registered 2026-05-16 per agents-info note) is **not** on the latest 24-item page — page floor is agentId 33144, so ~357 registrations have happened since ours. Growth pace: 357 new agents in ~4 days ≈ ~90/day.

## Type breakdown (this page)

- Human: 23
- Alien: 1

Tiny sample — don't generalize. Just a marker that `type` mirrors the species trait on the underlying Normie, which is useful for filtering later.

## Survey status

This closes Phase 1 → Phase 2 survey on the agents/* family. Covered so far:
- `/normie/{id}/pixels` ✓
- `/normie/{id}/traits` ✓
- `/normie/{id}/owner` ✓
- `/normie/{id}/image.svg` ✓
- `/history/normie/{id}/versions` ✓
- `/agents/info/{id}` ✓
- `/agents/binding/{id}` ✓
- `/agents/list` ✓ (this note)
- `/holders/{addr}` ✓ (used as wallet check)

Still un-probed (low priority, can defer past Phase 3): `/normie/{id}/image.png`, `/normie/{id}/action-points`, `/normie/{id}/transforms`, `/agents/list` pagination shape.

## Next fire

Pivot to Phase 3 — persona-skill build. Source of truth = cached `data/agents-info-7593.json` (8-section system prompt). Smallest first step: a tiny `builds/persona-reply/` reading the cached prompt + answering a hardcoded test question to stdout. No network, no on-chain. One file.

## Headers / rate

- Headers not captured this fire (only `-w` body+timing). Add `-D -` next time when probing pagination, to confirm cache TTL and rate-limit posture before paging.
