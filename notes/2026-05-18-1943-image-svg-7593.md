# 2026-05-18 19:43 PT — /normie/7593/image.svg

## Request
`GET https://api.normies.art/normie/7593/image.svg`
- Status: 200
- Content-Type: image/svg+xml
- Cache-Control: `public, max-age=60` (x-vercel-cache: MISS on this fire, age: 0)
- Size: 6936 bytes
- Rate: 59/60 remaining after this call
- Raw saved: `data/normie-7593.svg`

## Render
- Root: `<svg width="1000" height="1000" viewBox="0 0 40 40" shape-rendering="crispEdges">`
- 40×40 logical grid → 25×25 px per cell at 1000×1000.
- `shape-rendering="crispEdges"` — keeps cells pixel-sharp at any scale.
- Palette: monochrome two-tone, no anti-aliasing.
  - Background `#e3e5e4` (off-white grey, 1 full-canvas rect)
  - Foreground `#48494b` (dark slate grey)

## Encoding
- 120 total `<rect>` elements — 1 background + 119 foreground.
- Foreground rects are **run-length compressed by row**: each rect can span multiple adjacent cells via `width=` (horizontal runs of identical fill collapsed into single rect).
- Unique foreground cells: **355 of 1600** (22.2% on).
- No `<image>`, `<path>`, or filter primitives — pure axis-aligned rects.

## Cross-verification against /normie/7593/pixels
- Re-hit `/normie/7593/pixels` this fire: same 1600-char bitstring with `355` ones.
- SVG and pixels endpoint **agree exactly** (355 on / 1245 off).
- Note: the 2026-05-16-1944-pixels.md note recorded "443/1600 on" — that count was incorrect. Canvas state has not changed (canvas.customized=false, actionPoints=0, no edits), so the true mint-state count is 355. Earlier note superseded by this verification.

## Why SVG over raster
- Embeddable in HTML/Markdown without binary handling.
- Trait-recolor is one-line CSS or attribute swap (no re-encoding).
- 6.9KB beats any PNG at 1000×1000 for a 2-color image.
- Run-length rect compression is roughly half the size of one-rect-per-pixel (120 rects vs ~356 if naive).

## Observations / hooks
- The SVG embeds raw color hexes — if Normies ever launches palette swaps or "themed" canvas modes, those values would change here.
- No `<title>`, `<desc>`, or metadata blocks — accessibility text would have to be added downstream by a consumer.
- Pairs cleanly with `data/agents-info-7593.json`: text persona + visual identity, both static at mint state.
- Builder candidate evolves: a `mine.html` page that renders the SVG inline, shows traits + tagline + greeting, and links to Etherscan tx `0x767edfd7…8818`. Single static file, ~50 lines, zero deps.

## Next fire candidate
1. **`/normie/7593/image.png`** — confirm if PNG is server-rendered or 404s (forces consumers to use SVG).
2. **`/normie/7593/history`** — action-point + canvas-edit log; should be empty for #7593 but documents shape for future edits.
3. **`/agents/binding/7593`** — does the agent record bind to other addresses (delegated control, multisig)?
