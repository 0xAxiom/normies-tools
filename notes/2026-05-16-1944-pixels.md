# 2026-05-16 19:44 PT — `/normie/{id}/pixels`

## Request
- URL: `https://api.normies.art/normie/1/pixels`
- Method: GET (no auth)
- Token tested: `1`

## Response
- `200 OK` in 311ms, 1600 bytes
- `content-type: text/plain; charset=UTF-8`
- `cache-control: public, max-age=60`
- `x-ratelimit-limit: 60`, `x-ratelimit-remaining: 59` (sliding window confirmed live)
- `access-control-allow-origin: *` — CORS open, browser-callable
- `x-vercel-cache: MISS` on first hit (so 60s edge cache is real, not always hot)

## Shape
- Raw `text/plain` body — NOT JSON.
- Exactly 1600 chars, every char is `0` or `1`. Row-major 40x40 grid (offset = `row*40 + col`).
- Token 1: 443 on-pixels / 1157 off-pixels (~27.7% fill).
- Visually a centered humanoid silhouette w/ helmet+visor and lower-body noise field — sane, not corrupted.

## Implications for build
- Consumer code is trivial: `for i in range(40): row = body[i*40:(i+1)*40]`.
- 1600 bytes is so small that a local mirror of all 10k pixel maps is only ~16 MB — feasible to cache if we ever want offline rendering.
- No content-encoding negotiation — server returns plaintext, not gzipped. Bandwidth-cheap but not compressed.
- Edge cache TTL 60s means polling for canvas edits is meaningful — same token can change between fires once Phase 2 owns one.

## Open questions deferred
- Does the response include any header signaling current canvas version? (No — `/history/normie/{id}/versions` endpoint will handle that.)
- Is the pixels output the current canvas state or the immutable base bitmap? Need to compare against a Normie with a known edit later.

## Next fire candidate
- Endpoint #2 from scaffold list: `/normie/{id}/traits` — should be JSON, will show trait schema.

## Rate budget used
- 1/60 this minute.
