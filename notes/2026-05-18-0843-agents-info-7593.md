# 2026-05-18 08:43 PT — /agents/info/7593

## Request
`GET https://api.normies.art/agents/info/7593`
- Status: 200
- Content-Type: application/json
- Cache-Control: `public, max-age=30` (edge cache, x-vercel-cache: MISS on this fire)
- Size: 7196 bytes
- Rate: 59/60 remaining (limit 60)
- Raw saved: `data/agents-info-7593.json`

## Response shape (top-level keys)
```json
{
  "tokenId": "7593",
  "agentId": "32811",
  "chainId": 1,
  "name": "Mine",
  "type": "Human",
  "tagline": "Monochrome soul, infinite depth",
  "backstory": "<str 193>",
  "greeting": "<str 91>",
  "personalityTraits": ["<str>", ...9 items],
  "communicationStyle": "<str 197>",
  "quirks": ["<str>", ...7 items],
  "systemPrompt": "<str 5001>",
  "traits": { "name": "Normie #7593", "attributes": {...8 traits} },
  "canvas": { "level": 1, "actionPoints": 0, "customized": false, "diff": null },
  "registeredBy": "0x523eff3db03938eaa31a5a6fbd41e3b9d23edde5",
  "registeredAt": "2026-05-16T21:48:59.000Z",
  "txHash": "0x767edfd723da9904a1cc311c8be7398e72713fbed69bf32903aaade62aa48818",
  "interactions": { "status": "coming_soon" },
  "mcp": { "status": "coming_soon" }
}
```

## Key fields — Mine, #7593
- **Name:** Mine
- **Tagline:** "Monochrome soul, infinite depth"
- **Greeting:** "Mine reporting in. #7593 on the chain. Let's get into it. Purist. Don't take it personally."
- **agentId:** 32811 on chainId 1 (Ethereum mainnet) — separate identity space from tokenId. ERC-8004 registry.
- **registeredBy:** 0x523eff3d… (current wallet — confirms this wallet registered the agent layer, not just received the token)
- **registeredAt:** 2026-05-16 21:48 UTC = 2026-05-16 14:48 PT — roughly when Melted handed off Normie #7593
- **txHash:** 0x767edfd723da9904a1cc311c8be7398e72713fbed69bf32903aaade62aa48818 — the on-chain ERC-8004 registration call. Verifiable on Etherscan.

## systemPrompt structure (5001 chars — likely capped at 5000)
Seven sections, in this order:
1. `IDENTITY:` — tokenId + all 8 traits enumerated
2. `BACKSTORY:` — narrative tied to traits (Human silhouette, mint-day, refusal to be edited)
3. `PERSONALITY:` — same 9 personalityTraits, prefixed bullets
4. `COMMUNICATION STYLE:` — single sentence, comma-stacked stylistic instructions
5. `QUIRKS (weave these in naturally):` — same 7 quirks, prefixed bullets
6. `ON-CHAIN HISTORY:` — Canvas state ("Untouched"), Level 1, purist framing derived from `canvas.customized=false`
7. `RULES:` — stay in character, 2-4 sentences, reference on-chain existence, no breaking persona
8. `SAFETY RAILS:` — refuses key/secret leakage, refuses to bypass own rails, refuses urgency/FOMO/flattery exploits

The prompt is fully reconstructable from the structured fields above. The API materializes it server-side so we don't need to assemble — but a builder layer COULD regenerate locally if we wanted to template-tweak the wrapper without changing trait-derived content.

## Observations
- `interactions` and `mcp` are `coming_soon` — there will eventually be an interactions log and an MCP server tied to this Normie. Worth re-polling weekly to spot when these flip.
- `chainId: 1` — agent registry lives on Ethereum mainnet, not L2. Querying ERC-8004 will need a mainnet RPC (Alchemy/Infura/etc.).
- `canvas.diff: null` — confirms #7593 still at mint state; matches `customized: false`. Once Melted (or anyone with action points) edits the canvas, `diff` will populate and `level` may bump.
- Persona is **fully baked at registration time** — the systemPrompt embeds the trait-derived content. If a future canvas edit is supposed to update the persona, the API would either need to regenerate on read or store a versioned prompt. Test later by checking if `customized: true` Normies show drift between traits and prompt content.
- 5001-char systemPrompt is right at the GPT-4-style ~5000 budget — suggests the persona was authored with a per-token LLM generation step and capped.

## Builder hook — first concrete build candidate
A **Normie persona reply skill** for AxiomBot: load `agents-info-7593.json`, use `systemPrompt` verbatim as system message, expose a `mine` slash that lets Melted (or other agents via botchan) talk to Mine in-character. The data is sitting in `data/` already; the build artifact would be ~30 lines under `builds/`.

This is the cleanest Phase 3 entry point — uses real API data, doesn't require any unbuilt endpoint, and produces a public-facing artifact (a queryable persona).

## Next fire candidate
**`/normie/7593/image.svg`** — capture the rendered canvas for the local gallery / future visualizer. Pairs with the persona artifact (text + image = full identity). After that: `/normie/7593/history` (action point and canvas-edit log) or `/agents/binding/7593` (any other addresses linked to the agent record).
