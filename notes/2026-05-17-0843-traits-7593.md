# 2026-05-17 08:43 PT — /normie/7593/traits

## Request
`GET https://api.normies.art/normie/7593/traits`
- Status: 200
- Content-Type: application/json
- Rate: 59/60 remaining
- Body: 395 bytes

## Response shape
```json
{
  "raw": "0x0000021309080507",
  "attributes": [
    { "trait_type": "Type",            "value": "Human" },
    { "trait_type": "Gender",          "value": "Male" },
    { "trait_type": "Age",             "value": "Old" },
    { "trait_type": "Hair Style",      "value": "Half Shaved" },
    { "trait_type": "Facial Feature",  "value": "Normal Beard" },
    { "trait_type": "Eyes",            "value": "Eye Mask" },
    { "trait_type": "Expression",      "value": "Confident" },
    { "trait_type": "Accessory",       "value": "Headband" }
  ]
}
```

## Observations
- `raw` is 8-byte hex (16 hex chars = 64 bits) → one byte per trait slot, 8 slots, matches `attributes` length.
- Slot order in raw maps left-to-right to the attribute order returned:
  - byte 0 `00` = Type → Human
  - byte 1 `00` = Gender → Male
  - byte 2 `02` = Age → Old
  - byte 3 `13` = Hair Style → Half Shaved
  - byte 4 `09` = Facial Feature → Normal Beard
  - byte 5 `08` = Eyes → Eye Mask
  - byte 6 `05` = Expression → Confident
  - byte 7 `07` = Accessory → Headband
- OpenSea-shaped `attributes` array — drop-in for metadata layers / marketplaces.
- No `image`/`description`/`name` here; this endpoint is traits-only. Image lives at `/normie/{id}/image.{svg,png}`. Persona lives at `/agents/info/{id}`.

## Persona seed (for later)
"Old Human Male, half-shaved, normal beard, eye-mask, confident, wearing a headband." Visually reads like a streetwise/operative archetype. Persona endpoint (Phase 2 follow-up) will tell us how the API itself frames it.

## Next fire candidate
`/normie/7593/owner` (cheap, verifies the wallet→token binding from the API side, complements the holders/ lookup we already have) OR jump to `/agents/info/7593` for the live persona. Owner is safer + faster; persona is the higher-value Phase 2 payload. Recommend owner next (1 req), then persona the fire after.
