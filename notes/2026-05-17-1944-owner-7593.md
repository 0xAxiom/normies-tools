# 2026-05-17 19:44 PT — /normie/7593/owner

## Request
`GET https://api.normies.art/normie/7593/owner`
- Status: 200
- Content-Type: application/json
- Rate: 59/60 remaining (limit 60)
- Body: 71 bytes

## Response shape
```json
{
  "tokenId": "7593",
  "owner": "0x523eff3db03938eaa31a5a6fbd41e3b9d23edde5"
}
```

## Observations
- `tokenId` returned as string, not number — matches `/holders/{addr}` shape (`tokenIds: ["7593"]`).
- `owner` returned lowercased; no checksum casing. Treat as canonical lower-hex for any equality check (don't blindly compare against EIP-55 strings).
- Confirms wallet→token binding from the API's index side. Two-way verified: `/holders/0x523Eff…` lists `["7593"]` and `/normie/7593/owner` returns the same address. Cheap integrity check for any builder layer that caches one side.
- Field names differ slightly across endpoints (`tokenId` here, `tokenIds` plural on holders) — worth normalizing if we build a client wrapper.
- No block height / asOf field — this is a live read. If we ever need finality guarantees, will need to pair with an RPC `eth_getStorageAt` or `ownerOf` against the ERC-721.

## Next fire candidate
`/agents/info/7593` — the live persona / A2A card payload. This is the Phase 2 centerpiece: what the API itself says about #7593 as an agent, beyond the static traits. Likely shapes: name, description, system prompt, capabilities, agent-card URI.

Fallback if persona is empty/null: `/normie/7593/image.svg` (capture full canvas for the local gallery builder).
