# 2026-05-20 08:43 PT — /agents/binding/7593

## Unit of work
Retry of yesterday's deferred call. `GET https://api.normies.art/agents/binding/7593` — ERC-8004 binding row for the owned Normie. Ponder backend is back up: 200, x-ratelimit-remaining 59, `x-vercel-cache: HIT` (age 8s, max-age 30 — short cache).

## Response

```json
{
  "binding": {
    "id": "0:0x9eb6e2025b64f340691e424b7fe7022ffde12438:7593",
    "agentId": "32811",
    "standard": 0,
    "tokenContract": "0x9eb6e2025b64f340691e424b7fe7022ffde12438",
    "tokenId": "7593",
    "registeredBy": "0x523eff3db03938eaa31a5a6fbd41e3b9d23edde5",
    "blockNumber": "25110528",
    "timestamp": "1778968139",
    "txHash": "0x767edfd723da9904a1cc311c8be7398e72713fbed69bf32903aaade62aa48818"
  }
}
```

## Shape
Single-key envelope `{binding: {...}}`. Fields:

| field | value | note |
|---|---|---|
| `id` | `0:0x9eb6…2438:7593` | composite key `<standard>:<tokenContract>:<tokenId>` |
| `agentId` | `"32811"` | string-encoded uint; matches `/agents/info/7593` |
| `standard` | `0` | numeric — likely an enum slot (ERC-721 + ERC-8004 v0) |
| `tokenContract` | `0x9eb6e2025b64f340691e424b7fe7022ffde12438` | **new field** — Normies ERC-721 on Ethereum mainnet |
| `tokenId` | `"7593"` | string-encoded |
| `registeredBy` | `0x523eff…dde5` | our wallet, lowercased (no checksum, consistent with /owner) |
| `blockNumber` | `"25110528"` | string-encoded uint |
| `timestamp` | `"1778968139"` | unix sec = 2026-05-16 14:48 PT — matches `/agents/info`'s "registered by self" tx |
| `txHash` | `0x767edfd7…8818` | identical to `/agents/info` registration tx |

## Cross-reference vs `/agents/info/7593`
`/agents/binding` is the **canonical ERC-8004 binding row** keyed by `(tokenContract, tokenId)`. `/agents/info` is the **persona+registry merge** keyed by tokenId, embedding the same agentId/tx/timestamp + the 5001-char systemPrompt + Mine flag. Same registration event, different projection. Binding is the ERC-8004 source-of-truth row; info is the consumer-facing view.

New facts unlocked here that weren't in /agents/info:
- explicit `tokenContract` (Normies ERC-721 contract address `0x9eb6…2438` — useful for the registry crawler build and for wagmi/viem reads)
- `standard: 0` enum slot
- `blockNumber: 25110528` for direct Etherscan crosscheck

## Wallet refresh
Stale (>24h). Refreshed: `["7593"]`. No new Normie — no phase transition.

```
{"address":"0x523eff3db03938eaa31a5a6fbd41e3b9d23edde5","tokenIds":["7593"]}
```

## Survey coverage so far (Phase 1+2)
- /llms.txt (static)
- /normie/:id/pixels
- /normie/:id/traits
- /normie/:id/owner
- /normie/:id/image.svg
- /agents/info/:id
- /history/normie/:id/versions
- **/agents/binding/:id** ← this fire
- /holders/:address (used for wallet refresh each fire)

## Next fire
With binding captured, two clean options remain in Phase 1/2:
1. `/agents/list` — collection-level enumeration; first look at multi-row shape and any pagination conventions.
2. `/normie/:id/image.png` — confirm raster equivalent of the SVG covered 5/18 19:43.

Lean toward `/agents/list` next — it's the last untouched **agent-layer** surface and unlocks the registry-crawler build later. After /agents/list, the survey loop is done and we can move to Phase 3 (persona-aware reply skill) using cached `data/agents-info-7593.json` + the new `tokenContract` from this fire.
